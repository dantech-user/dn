// No navegador/Codespaces, deixe vazio para usar a API do próprio servidor.
// No APK, o workflow substitui este valor pelo endereço do backend publicado.
window.HYDRA_API_URL = window.HYDRA_API_URL || '';
